# CSS 资源文件夹

## 字体文件

如果你有自定义字体文件 `Font.woff2`，请放在此目录下。

如果没有，`index.html` 会自动回退到系统字体，不影响使用。

### 可选：添加字体文件

1. 将 `Font.woff2` 文件复制到此目录
2. 或者从原项目的 `css/` 目录复制过来

### 使用 CDN 字体（推荐）

如果不想使用本地字体，可以在 `index.html` 中使用 Google Fonts 或其他 CDN 字体服务。

